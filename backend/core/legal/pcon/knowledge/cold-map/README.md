# Cold map — failure citizens

Negative cartography for Private Confidant. Failures are first-class and citable.

- Schema: `schema.json`
- Entries: `entries/*.json`
- Status `active` = still burn the agent if repeated
- Do not auto-merge; human curates

Epistemology: failure is stickier than philosophy of “correct.” Shrink known bad basins under read-audit.
